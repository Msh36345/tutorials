# מדריך מקיף: עבודה עם מחרוזות (Strings) ב-Polars

## 📚 תוכן עניינים

1. [מבוא](#מבוא)
2. [סינון שורות לפי תנאים](#סינון-שורות-לפי-תנאים)
3. [המרת מחרוזות לתאריכים ושעות](#המרת-מחרוזות-לתאריכים-ושעות)
4. [חילוץ חלקי מחרוזת](#חילוץ-חלקי-מחרוזת)
5. [ניקוי וטיפול במחרוזות](#ניקוי-וטיפול-במחרוזות)
6. [פיצול מחרוזות לרשימות](#פיצול-מחרוזות-לרשימות)
7. [שרשור מחרוזות](#שרשור-מחרוזות)
8. [תרגילים מומלצים](#תרגילים-מומלצים)
9. [טיפים וטריקים](#טיפים-וטריקים)
10. [משאבים נוספים](#משאבים-נוספים)

---

## 🎯 מבוא

פרק זה מתמקד בעבודה עם מחרוזות ב-Polars - ספריית Python מהירה ויעילה לעיבוד נתונים. נלמד כיצד לטפל במחרוזות בצורה אופטימלית ומקצועית.

### דרישות מקדימות
```python
import polars as pl
import os
```

### ⚙️ הגדרות בסיסיות

לפני שמתחילים, כדאי להגדיר את אורך התצוגה של מחרוזות:

```python
# להגדרת אורך תצוגה של מחרוזות
os.environ['POLARS_FMT_STR_LEN'] = str(50)
```

---

## 1️⃣ סינון שורות לפי תנאים

### 📖 הסבר

סינון שורות לפי תנאים על מחרוזות הוא אחת הפעולות הנפוצות ביותר בעיבוד נתונים. Polars מספק מגוון רחב של פונקציות לסינון מחרוזות.

### 🔍 1.1 סינון לפי תחילת מחרוזת

**מתי להשתמש?** כאשר אתה רוצה למצוא שורות שמתחילות במילה או תבנית מסוימת.

```python
import polars as pl

# טעינת הנתונים
df = pl.read_csv('../data/google_store_reviews.csv')

# סינון ביקורות שמתחילות במילה "Very"
result = (
    df
    .filter(pl.col('content').str.starts_with('Very'))
    .select('content')
    .head()
)
```

**פלט צפוי:**
```
shape: (5, 1)
│ content                                             │
│ ---                                                 │
│ str                                                 │
╞═════════════════════════════════════════════════════╡
│ Very costly for the premium version (approx Indian… │
│ Very bad                                            │
│ Very bad                                            │
│ Very slow app it took me 10 min just to start the … │
│ Very average app, not intuitive . Will probably sp… │
```

**💡 הסבר שלב אחר שלב:**
1. `pl.col('content')` - בוחר את עמודת התוכן
2. `.str.starts_with('Very')` - בודק אם המחרוזת מתחילה ב-"Very"
3. `.filter()` - מסנן רק שורות שעונות על התנאי
4. `.select('content')` - בוחר רק את עמודת התוכן לתצוגה

---

### 🔍 1.2 סינון לפי סיומת מחרוזת

**מתי להשתמש?** למציאת שמות משפחה, סיומות קבצים, או דפוסים בסוף מחרוזת.

```python
# סינון משתמשים ששם המשפחה שלהם Smith
result = (
    df
    .filter(pl.col('userName').str.ends_with('Smith'))
    .select('userName')
    .head()
)
```

**פלט צפוי:**
```
shape: (5, 1)
│ userName     │
│ ---          │
│ str          │
╞══════════════╡
│ James Smith  │
│ J Smith      │
│ Fj Smith     │
│ Martyn Smith │
│ D Smith      │
```

---

### 🔍 1.3 סינון לפי מחרוזת מדויקת

**מתי להשתמש?** כאשר אתה מחפש מילה או ביטוי ספציפי בתוך המחרוזת.

```python
# חיפוש המילה "happy" בביקורות
result = (
    df
    .filter(pl.col('content').str.contains('happy', literal=True))
    .select('content')
    .head()
)
```

**פרמטרים חשובים:**
- `literal=True` - מחפש את המחרוזת המדויקת (לא כביטוי רגולרי)
- `literal=False` (ברירת מחדל) - מתייחס לפרמטר כביטוי רגולרי

**פלט צפוי:**
```
shape: (5, 1)
│ content                                             │
│ ---                                                 │
│ str                                                 │
╞═════════════════════════════════════════════════════╡
│ I love this app, but I do have one major gripe - I… │
│ Not happy, app just asked me to 'sign in' and now … │
│ Will be happy if this app comes with time duration… │
│ V usefull app i love it v much I use it daily work… │
│ I was super happy to download this app but that I … │
```

---

### 🔍 1.4 סינון עם ביטויים רגולריים (Regex)

**מתי להשתמש?** לחיפוש מורכב עם מספר אפשרויות או דפוסים.

```python
# חיפוש ביקורות עם אחד מהביטויים: "very happy", "best app", "I love"
result = (
    df
    .filter(pl.col('content').str.contains(r'very happy|best app|I love'))
    .select('content')
    .head()
)
```

**💡 תחביר Regex:**
- `|` - אופרטור OR (או)
- `r'...'` - raw string (למניעת בעיות עם תווים מיוחדים)

**פלט צפוי:**
```
shape: (5, 1)
│ content                                             │
│ ---                                                 │
│ str                                                 │
╞═════════════════════════════════════════════════════╡
│ I love this app, but I do have one major gripe - I… │
│ Why are random items popping up on our Grocery Lis… │
│ I love using this app however when I installed it … │
│ There are certain things I love like the fact that… │
│ I love this app, but recently the app keeps crashi… │
```

---

### 🔍 1.5 סינון עם מספר מילות מפתח

**מתי להשתמש?** לחיפוש מהיר של מספר מילים ללא צורך בביטוי רגולרי מורכב.

```python
# חיפוש ביקורות המכילות אחת מהמילים: happy, love, best
count = (
    df
    .filter(pl.col('content').str.contains_any(['happy', 'love', 'best']))
    .height
)
print(f"מספר שורות שנמצאו: {count}")
```

**פלט צפוי:**
```
מספר שורות שנמצאו: 1237
```

**יתרונות:**
- ✅ תחביר פשוט וברור
- ✅ מהיר יותר מביטוי רגולרי
- ✅ קל לתחזוקה

---

### 🔍 1.6 ספירת התאמות

**מתי להשתמש?** כאשר רוצים לדעת כמה פעמים דפוס מופיע במחרוזת.

```python
# מציאת ביקורות שמכילות יותר מ-2 התאמות של הביטויים
result = (
    df
    .filter(pl.col('content').str.count_matches(r'very happy|best app|I love') > 2)
    .select('content')
)
```

**פלט צפוי:**
```
shape: (4, 1)
│ content                                             │
│ ---                                                 │
│ str                                                 │
╞═════════════════════════════════════════════════════╡
│ I love it :D I met great and fun people and this i… │
│ I love Habitica! I've used it for several years, b… │
│ A lot of work was put into this. I love the idea o… │
│ Very nice app I downloaded many app but it is the … │
```

---

### 🔍 1.7 סינון לפי אורך מחרוזת

**מתי להשתמש?** לזיהוי שמות ארוכים, תיאורים קצרים, או ולידציה.

```python
# מציאת שמות משתמשים עם יותר מ-10 תווים
result = (
    df
    .filter(pl.col('userName').str.len_chars() > 10)
    .select('userName')
    .head()
)
```

**פלט צפוי:**
```
shape: (5, 1)
│ userName                         │
│ ---                              │
│ str                              │
╞══════════════════════════════════╡
│ Sudhakar .S                      │
│ SKGflorida@bellsouth.net DAVID S │
│ Louann Stoker                    │
│ Jon Clemens                      │
│ I Dewa Gede Nopi Ariana          │
```

**💡 הבדל חשוב:**
- `.str.len_chars()` - מונה תווים (אותיות, ספרות, סימנים)
- `.str.len_bytes()` - מונה בתים (רלוונטי לקידוד תווים)

---

## 2️⃣ המרת מחרוזות לתאריכים ושעות

### 📖 הסבר

המרת מחרוזות לטיפוסי תאריך ושעה היא קריטית לעיבוד נתונים טמפורליים. Polars תומך בהמרות גמישות ומהירות.

### 🗓️ 2.1 המרה לתאריך (Date)

```python
# המרת עמודת תאריך לטיפוס Date
result = df.select(
    'at',
    pl.col('at').str.to_date(format='%Y-%m-%d %H:%M:%S').alias('at(date)')
).head()
```

**פלט צפוי:**
```
shape: (5, 2)
│ at                  ┆ at(date)   │
│ ---                 ┆ ---        │
│ str                 ┆ date       │
╞═════════════════════╪════════════╡
│ 2020-10-27 21:24:41 ┆ 2020-10-27 │
│ 2020-10-27 14:03:28 ┆ 2020-10-27 │
│ 2020-10-27 08:18:40 ┆ 2020-10-27 │
│ 2020-10-26 13:28:07 ┆ 2020-10-26 │
│ 2020-10-26 06:10:50 ┆ 2020-10-26 │
```

---

### ⏰ 2.2 המרה לשעה (Time)

```python
# המרת עמודת תאריך לטיפוס Time (רק השעה)
result = df.select(
    'at',
    pl.col('at').str.to_time(format='%Y-%m-%d %H:%M:%S').alias('at(time)')
).head()
```

**פלט צפוי:**
```
shape: (5, 2)
│ at                  ┆ at(time) │
│ ---                 ┆ ---      │
│ str                 ┆ time     │
╞═════════════════════╪══════════╡
│ 2020-10-27 21:24:41 ┆ 21:24:41 │
│ 2020-10-27 14:03:28 ┆ 14:03:28 │
│ 2020-10-27 08:18:40 ┆ 08:18:40 │
│ 2020-10-26 13:28:07 ┆ 13:28:07 │
│ 2020-10-26 06:10:50 ┆ 06:10:50 │
```

---

### 📅 2.3 המרה לתאריך ושעה (Datetime)

```python
# המרה מלאה לטיפוס Datetime
result = df.select(
    'at',
    pl.col('at').str.to_datetime(format='%Y-%m-%d %H:%M:%S').alias('at(datetime)')
).head()
```

**פלט צפוי:**
```
shape: (5, 2)
│ at                  ┆ at(datetime)        │
│ ---                 ┆ ---                 │
│ str                 ┆ datetime[μs]        │
╞═════════════════════╪═════════════════════╡
│ 2020-10-27 21:24:41 ┆ 2020-10-27 21:24:41 │
│ 2020-10-27 14:03:28 ┆ 2020-10-27 14:03:28 │
│ 2020-10-27 08:18:40 ┆ 2020-10-27 08:18:40 │
│ 2020-10-26 13:28:07 ┆ 2020-10-26 13:28:07 │
│ 2020-10-26 06:10:50 ┆ 2020-10-26 06:10:50 │
```

---

### 🎯 2.4 שימוש ב-strptime (גמיש יותר)

```python
# המרה גמישה לכל טיפוסי הזמן
result = df.select(
    'at',
    pl.col('at').str.strptime(pl.Date, '%Y-%m-%d %H:%M:%S').alias('at(date)'),
    pl.col('at').str.strptime(pl.Time, '%Y-%m-%d %H:%M:%S').alias('at(time)'),
    pl.col('at').str.strptime(pl.Datetime, '%Y-%m-%d %H:%M:%S').alias('at(datetime)')
).head()
```

**💡 יתרונות strptime:**
- גמישות מלאה בבחירת טיפוס היעד
- תמיכה בפורמטים מורכבים
- שגיאות ברורות יותר

---

### 📋 טבלת סימני פורמט נפוצים

| סימן | משמעות | דוגמה |
|------|---------|-------|
| `%Y` | שנה (4 ספרות) | 2020 |
| `%m` | חודש (2 ספרות) | 01-12 |
| `%d` | יום (2 ספרות) | 01-31 |
| `%H` | שעה (24 שעות) | 00-23 |
| `%M` | דקות | 00-59 |
| `%S` | שניות | 00-59 |
| `%B` | שם חודש מלא | January |
| `%b` | שם חודש מקוצר | Jan |
| `%A` | שם יום מלא | Monday |
| `%a` | שם יום מקוצר | Mon |

---

## 3️⃣ חילוץ חלקי מחרוזת

### 📖 הסבר

חילוץ חלקים ממחרוזת הוא כלי חיוני לעיבוד טקסט. Polars מספק מספר שיטות לחילוץ - מאינדקס פשוט ועד ביטויים רגולריים מורכבים.

### ✂️ 3.1 חיתוך בסיסי (Slice)

**מתי להשתמש?** כאשר אתה יודע בדיוק היכן נמצא המידע שאתה צריך.

```python
# חילוץ כל התווים מהתו הרביעי ואילך
result = df.select(
    'userName',
    pl.col('userName').str.slice(3).alias('4thCharAndAfter')
).head()
```

**פלט צפוי:**
```
shape: (5, 2)
│ userName                         ┆ 4thCharAndAfter               │
│ ---                              ┆ ---                           │
│ str                              ┆ str                           │
╞══════════════════════════════════╪═══════════════════════════════╡
│ Eric Tie                         ┆ c Tie                         │
│ john alpha                       ┆ n alpha                       │
│ Sudhakar .S                      ┆ hakar .S                      │
│ SKGflorida@bellsouth.net DAVID S ┆ florida@bellsouth.net DAVID S │
│ Louann Stoker                    ┆ ann Stoker                    │
```

**💡 הסבר פרמטרים:**
- `slice(3)` - מתחיל מתו מספר 3 (אינדקס 0-based)
- התו הראשון הוא אינדקס 0

---

### ✂️ 3.2 חיתוך עם אורך

```python
# חילוץ 5 תווים החל מהתו הרביעי
result = df.select(
    'userName',
    pl.col('userName').str.slice(3, 5).alias('5CharsAfter4thChar')
).head()
```

**פלט צפוי:**
```
shape: (5, 2)
│ userName                         ┆ 5CharsAfter4thChar │
│ ---                              ┆ ---                │
│ str                              ┆ str                │
╞══════════════════════════════════╪════════════════════╡
│ Eric Tie                         ┆ c Tie              │
│ john alpha                       ┆ n alp              │
│ Sudhakar .S                      ┆ hakar              │
│ SKGflorida@bellsouth.net DAVID S ┆ flori              │
│ Louann Stoker                    ┆ ann S              │
```

**תחביר:** `slice(start, length)`
- `start` - תו ההתחלה
- `length` - מספר התווים להחזיר

---

### ✂️ 3.3 חיתוך מהסוף (אינדקס שלילי)

```python
# חילוץ התו השני מהסוף
result = df.select(
    'userName',
    pl.col('userName').str.slice(-2, 1).alias('TheLastToSecondChar')
).head()
```

**פלט צפוי:**
```
shape: (5, 2)
│ userName                         ┆ TheLastToSecondChar │
│ ---                              ┆ ---                 │
│ str                              ┆ str                 │
╞══════════════════════════════════╪═════════════════════╡
│ Eric Tie                         ┆ i                   │
│ john alpha                       ┆ h                   │
│ Sudhakar .S                      ┆ .                   │
│ SKGflorida@bellsouth.net DAVID S ┆                     │
│ Louann Stoker                    ┆ e                   │
```

**💡 טיפ:** אינדקס שלילי סופר מהסוף
- `-1` הוא התו האחרון
- `-2` הוא התו השני מהסוף

---

### 🔎 3.4 חילוץ עם Regex - התאמה ראשונה

**מתי להשתמש?** כאשר אתה מחפש דפוס ספציפי.

```python
# חילוץ המילה הראשונה (אותיות בלבד)
result = df.select(
    'content',
    pl.col('content')
    .str.extract(r'([A-Za-z]+)')
    .alias('extract')
).head(5)
```

**פלט צפוי:**
```
shape: (5, 2)
│ content                                             ┆ extract │
│ ---                                                 ┆ ---     │
│ str                                                 ┆ str     │
╞═════════════════════════════════════════════════════╪═════════╡
│ I cannot open the app anymore                       ┆ I       │
│ I have been begging for a refund from this app for… ┆ I       │
│ Very costly for the premium version (approx Indian… ┆ Very    │
│ Used to keep me organized, but all the 2020 UPDATE… ┆ Used    │
│ Dan Birthday Oct 28                                 ┆ Dan     │
```

**💡 הסבר תבנית Regex:**
- `[A-Za-z]+` - אות אחת או יותר (גדולה או קטנה)
- סוגריים `()` - מגדירים קבוצת לכידה (capture group)

---

### 🔎 3.5 חילוץ עם קבוצות לכידה

```python
# חילוץ דפוס מורכב: 3 אותיות + רווח + מספר
result = df.select(
    'content',
    pl.col('content')
    .str.extract(r'([A-Za-z]{3}) ([0-9]+)', 0)
    .alias('extract whole matches'),
    pl.col('content')
    .str.extract(r'([A-Za-z]{3}) ([0-9]+)', 1)
    .alias('extract group 1'),
    pl.col('content')
    .str.extract(r'([A-Za-z]{3}) ([0-9]+)', 2)
    .alias('extract group 2')
).head(5)
```

**פלט צפוי:**
```
shape: (5, 4)
│ content                 ┆ extract whole matches ┆ extract group 1 ┆ extract group 2 │
│ ---                     ┆ ---                   ┆ ---             ┆ ---             │
│ str                     ┆ str                   ┆ str             ┆ str             │
╞═════════════════════════╪═══════════════════════╪═════════════════╪═════════════════╡
│ I cannot open...        ┆ null                  ┆ null            ┆ null            │
│ Very costly...          ┆ ees 910               ┆ ees             ┆ 910             │
│ Used to keep...         ┆ the 2020              ┆ the             ┆ 2020            │
│ Dan Birthday Oct 28     ┆ Oct 28                ┆ Oct             ┆ 28              │
```

**פרמטר קבוצה:**
- `0` - כל ההתאמה
- `1` - קבוצה ראשונה `()`
- `2` - קבוצה שנייה `()`

---

### 🔎 3.6 חילוץ כל ההתאמות

```python
# חילוץ כל המילים מהטקסט
result = df.select(
    'content',
    pl.col('content')
    .str.extract(r'([A-Za-z]+)')
    .alias('extract'),  # רק המילה הראשונה
    pl.col('content')
    .str.extract_all(r'([A-Za-z]+)')
    .alias('extract_all')  # כל המילים
).head(5)
```

**פלט צפוי:**
```
shape: (5, 3)
│ content                    ┆ extract ┆ extract_all                    │
│ ---                        ┆ ---     ┆ ---                            │
│ str                        ┆ str     ┆ list[str]                      │
╞════════════════════════════╪═════════╪════════════════════════════════╡
│ I cannot open the app...   ┆ I       ┆ ["I", "cannot", … "anymore"]   │
│ I have been begging...     ┆ I       ┆ ["I", "have", … "me"]          │
│ Very costly for the...     ┆ Very    ┆ ["Very", "costly", … "better"] │
│ Used to keep me...         ┆ Used    ┆ ["Used", "to", … "salary"]     │
│ Dan Birthday Oct 28        ┆ Dan     ┆ ["Dan", "Birthday", "Oct"]     │
```

**💡 הבדל מרכזי:**
- `extract()` - מחזיר STRING עם ההתאמה הראשונה
- `extract_all()` - מחזיר LIST עם כל ההתאמות

---

### 🔎 3.7 חילוץ עם extract_groups (מבנה מובנה)

```python
# חילוץ מספר קבוצות למבנה
result = df.select(
    'content',
    pl.col('content')
    .str.extract(r'([A-Za-z]{3}) ([0-9]+)', 0)
    .alias('extract'),
    pl.col('content')
    .str.extract_groups(r'([A-Za-z]{3}) ([0-9]+)')
    .alias('extract_groups')
).head()
```

**פלט צפוי:**
```
shape: (5, 3)
│ content                 ┆ extract  ┆ extract_groups    │
│ ---                     ┆ ---      ┆ ---               │
│ str                     ┆ str      ┆ struct[2]         │
╞═════════════════════════╪══════════╪═══════════════════╡
│ I cannot open...        ┆ null     ┆ {null,null}       │
│ Very costly...          ┆ ees 910  ┆ {"ees","910"}     │
│ Used to keep...         ┆ the 2020 ┆ {"the","2020"}    │
│ Dan Birthday Oct 28     ┆ Oct 28   ┆ {"Oct","28"}      │
```

**💡 יתרונות:**
- מחזיר struct עם כל הקבוצות
- נוח לגישה מובנית לנתונים
- תואם למבני נתונים מורכבים

---

### 🎯 3.8 Regex עם דגלים (Flags)

```python
# חיפוש לא תלוי רישיות (case-insensitive)
result = df.select(
    'content',
    pl.col('content')
    .str.extract_all(r'(?i)([A-Z]+)')
    .alias('extract_all')
).head()
```

**דגלים נפוצים:**
- `(?i)` - case-insensitive (לא רגיש לרישיות)
- `(?m)` - multiline mode
- `(?s)` - dotall mode (. תואם גם \n)

---

## 4️⃣ ניקוי וטיפול במחרוזות

### 📖 הסבר

ניקוי מחרוזות הוא חיוני להכנת נתונים לניתוח. Polars מציע כלים רבי עוצמה לניקוי, תיקון ועיצוב מחרוזות.

### 🧹 4.1 הסרת רווחים (Strip)

```python
df = pl.DataFrame({
    'text': [
        '  I aM a HUmAn.  ',
        'it is NOT   easy!  ',
        ' WHY are You cool'
    ]
})

# הסרת רווחים מהתחלה וסוף
result = df.select(
    'text',
    pl.col('text').str.strip_chars().alias('stripped_text')
)
```

**פלט צפוי:**
```
shape: (3, 2)
│ text                ┆ stripped_text     │
│ ---                 ┆ ---               │
│ str                 ┆ str               │
╞═════════════════════╪═══════════════════╡
│   I aM a HUmAn.     ┆ I aM a HUmAn.     │
│ it is NOT   easy!   ┆ it is NOT   easy! │
│  WHY are You cool   ┆ WHY are You cool  │
```

**💡 וריאציות:**
- `.str.strip_chars()` - רווחים מהתחלה והסוף
- `.str.strip_chars_start()` - רק מהתחלה
- `.str.strip_chars_end()` - רק מהסוף

---

### 🔄 4.2 החלפת טקסט - התאמה ראשונה

```python
# החלפת ההופעה הראשונה של 'a' ב-'new_a'
result = df.select(
    'text',
    pl.col('text')
    .str.replace('a', 'new_a', literal=True, n=1)
    .alias('replaced_text')
)
```

**פלט צפוי:**
```
shape: (3, 2)
│ text                ┆ replaced_text           │
│ ---                 ┆ ---                     │
│ str                 ┆ str                     │
╞═════════════════════╪═════════════════════════╡
│   I aM a HUmAn.     ┆   I new_aM a HUmAn.     │
│ it is NOT   easy!   ┆ it is NOT   enew_asy!   │
│  WHY are You cool   ┆  WHY new_are You cool   │
```

**פרמטרים:**
- `literal=True` - התייחס לטקסט כמחרוזת מדויקת
- `n=1` - החלף רק הופעה אחת

---

### 🔄 4.3 החלפת כל ההופעות

```python
# החלפת כל ההופעות של 'a'
result = df.select(
    'text',
    pl.col('text')
    .str.replace_all('a', 'new_a', literal=True)
    .alias('replaced_all_text')
)
```

**פלט צפוי:**
```
shape: (3, 2)
│ text                ┆ replaced_all_text         │
│ ---                 ┆ ---                       │
│ str                 ┆ str                       │
╞═════════════════════╪═══════════════════════════╡
│   I aM a HUmAn.     ┆   I new_aM new_a HUmAn.   │
│ it is NOT   easy!   ┆ it is NOT   enew_asy!     │
│  WHY are You cool   ┆  WHY new_are You cool     │
```

---

### 🔤 4.4 שינוי רישיות - Title Case

```python
# המרה ל-Title Case (אות ראשונה גדולה בכל מילה)
result = df.select(
    'text',
    pl.col('text').str.to_titlecase().alias('title_case')
)
```

**פלט צפוי:**
```
shape: (3, 2)
│ text                ┆ title_case          │
│ ---                 ┆ ---                 │
│ str                 ┆ str                 │
╞═════════════════════╪═════════════════════╡
│   I aM a HUmAn.     ┆   I Am A Human.     │
│ it is NOT   easy!   ┆ It Is Not   Easy!   │
│  WHY are You cool   ┆  Why Are You Cool   │
```

---

### 🔤 4.5 אותיות קטנות (Lowercase)

```python
result = df.select(
    'text',
    pl.col('text').str.to_lowercase().alias('lower_case')
)
```

**פלט צפוי:**
```
shape: (3, 2)
│ text                ┆ lower_case          │
│ ---                 ┆ ---                 │
│ str                 ┆ str                 │
╞═════════════════════╪═════════════════════╡
│   I aM a HUmAn.     ┆   i am a human.     │
│ it is NOT   easy!   ┆ it is not   easy!   │
│  WHY are You cool   ┆  why are you cool   │
```

---

### 🔤 4.6 אותיות גדולות (Uppercase)

```python
result = df.select(
    'text',
    pl.col('text').str.to_uppercase().alias('upper_case')
)
```

**פלט צפוי:**
```
shape: (3, 2)
│ text                ┆ upper_case          │
│ ---                 ┆ ---                 │
│ str                 ┆ str                 │
╞═════════════════════╪═════════════════════╡
│   I aM a HUmAn.     ┆   I AM A HUMAN.     │
│ it is NOT   easy!   ┆ IT IS NOT   EASY!   │
│  WHY are You cool   ┆  WHY ARE YOU COOL   │
```

---

### 📏 4.7 ריפוד (Padding)

**מתי להשתמש?** ליצירת עמודות מיושרות או הוספת אפסים מובילים.

```python
# ריפוד משמאל ומימין
result = df.select(
    'text',
    pl.col('text').str.pad_start(20, '~').alias('pad_start'),
    pl.col('text').str.pad_end(20, '~').alias('pad_end')
)
```

**פלט צפוי:**
```
shape: (3, 3)
│ text                ┆ pad_start            ┆ pad_end              │
│ ---                 ┆ ---                  ┆ ---                  │
│ str                 ┆ str                  ┆ str                  │
╞═════════════════════╪══════════════════════╪══════════════════════╡
│   I aM a HUmAn.     ┆ ~~~  I aM a HUmAn.   ┆   I aM a HUmAn.  ~~~ │
│ it is NOT   easy!   ┆ ~it is NOT   easy!   ┆ it is NOT   easy!  ~ │
│  WHY are You cool   ┆ ~~~ WHY are You cool ┆  WHY are You cool~~~ │
```

---

### 🔢 4.8 ריפוד עם אפסים (Zero Padding)

```python
# הוספת אפסים מובילים - נפוץ במספרי זיהוי
result = df.select(
    'text',
    pl.col('text').str.pad_start(20, '0').alias('pad_start')
)
```

**פלט צפוי:**
```
shape: (3, 2)
│ text                ┆ pad_start            │
│ ---                 ┆ ---                  │
│ str                 ┆ str                  │
╞═════════════════════╪══════════════════════╡
│   I aM a HUmAn.     ┆ 000  I aM a HUmAn.   │
│ it is NOT   easy!   ┆ 0it is NOT   easy!   │
│  WHY are You cool   ┆ 000 WHY are You cool │
```

**💡 שימושים נפוצים:**
- מספרי חשבון: `00012345`
- קודי מוצר: `SKU0001234`
- תאריכים: `2024-01-05`

---

## 5️⃣ פיצול מחרוזות לרשימות

### 📖 הסבר

פיצול מחרוזות הוא חיוני לעיבוד טקסט מובנה. Polars מציע כלים גמישים לפיצול פשוט ומורכב.

### ✂️ 5.1 פיצול בסיסי

```python
# פיצול לפי רווח
result = df.select(
    'content',
    pl.col('content').str.split(by=' ').alias('split')
).head()
```

**פלט צפוי:**
```
shape: (5, 2)
│ content                                ┆ split                           │
│ ---                                    ┆ ---                             │
│ str                                    ┆ list[str]                       │
╞════════════════════════════════════════╪═════════════════════════════════╡
│ I cannot open the app anymore          ┆ ["I", "cannot", … "anymore"]    │
│ I have been begging for a refund...    ┆ ["I", "have", … "me"]           │
│ Very costly for the premium version... ┆ ["Very", "costly", … "better."] │
```

**💡 טיפ:** התוצאה היא עמודת LIST, ניתן להמשיך ולעבוד איתה!

---

### ✂️ 5.2 פיצול עם הגבלת מספר חלקים

```python
# פיצול למקסימום 10 חלקים
result = df.select(
    'content',
    pl.col('content').str.splitn(by=' ', n=10).alias('splitn'),
    pl.col('content').str.split_exact(by=' ', n=10).alias('split_exact')
).head()
```

**פלט צפוי:**
```
shape: (5, 3)
│ content            ┆ splitn                    ┆ split_exact              │
│ ---                ┆ ---                       ┆ ---                      │
│ str                ┆ struct[10]                ┆ struct[11]               │
╞════════════════════╪═══════════════════════════╪══════════════════════════╡
│ I cannot open...   ┆ {"I","cannot",...,null}   ┆ {"I","cannot",...,null}  │
│ I have been...     ┆ {"I","have",...,"me"}     ┆ {"I","have",...,"for"}   │
```

**💡 ההבדל:**
- `splitn()` - מפצל עד n חלקים, השארית נשארת ביחד
- `split_exact()` - מפצל בדיוק ל-n חלקים, זורק שגיאה אם לא מתאים

---

### 📊 5.3 פיצול עם גישה לאיבר ספציפי

```python
# חילוץ המילה השלישית
result = df.select(
    'content',
    pl.col('content').str.split(by=' ').list.get(2).alias('third_word')
).head()
```

**💡 שילוב עם פעולות LIST:**
```python
# מספר המילים בכל משפט
result = df.select(
    'content',
    pl.col('content').str.split(by=' ').list.len().alias('word_count')
).head()
```

---

## 6️⃣ שרשור מחרוזות

### 📖 הסבר

שרשור (Concatenation) של מחרוזות הוא יצירת מחרוזת חדשה משילוב של מספר מחרוזות או עמודות.

### ➕ 6.1 שרשור פשוט עם אופרטור +

```python
df = pl.DataFrame({
    'colA': ['a', 'b', 'c', 'd'],
    'colB': ['aa', 'bb', 'cc', 'dd']
})

# שרשור עם טקסט קבוע
result = df.select(
    pl.all(),
    (pl.col('colB') + ' new').alias('newColB')
)
```

**פלט צפוי:**
```
shape: (4, 3)
│ colA ┆ colB ┆ newColB │
│ ---  ┆ ---  ┆ ---     │
│ str  ┆ str  ┆ str     │
╞══════╪══════╪═════════╡
│ a    ┆ aa   ┆ aa new  │
│ b    ┆ bb   ┆ bb new  │
│ c    ┆ cc   ┆ cc new  │
│ d    ┆ dd   ┆ dd new  │
```

---

### ➕ 6.2 שרשור בין עמודות

```python
# שרשור שתי עמודות
result = df.select(
    pl.all(),
    (pl.col('colA') + pl.col('colB')).alias('colC')
)
```

**פלט צפוי:**
```
shape: (4, 3)
│ colA ┆ colB ┆ colC │
│ ---  ┆ ---  ┆ ---  │
│ str  ┆ str  ┆ str  │
╞══════╪══════╪══════╡
│ a    ┆ aa   ┆ aaa  │
│ b    ┆ bb   ┆ bbb  │
│ c    ┆ cc   ┆ ccc  │
│ d    ┆ dd   ┆ ddd  │
```

---

### 🎯 6.3 concat_str - שרשור מתקדם

**מתי להשתמש?** כאשר צריך לשרשר מספר עמודות עם מפריד.

```python
# שרשור עם מפריד
result = df.select(
    pl.all(),
    pl.concat_str(
        pl.lit(100) + 3,  # ניתן להוסיף חישובים
        pl.lit(' '),
        pl.col('colA'),
        pl.col('colB'),
        separator='::'
    ).alias('newCol')
)
```

**פלט צפוי:**
```
shape: (4, 3)
│ colA ┆ colB ┆ newCol        │
│ ---  ┆ ---  ┆ ---           │
│ str  ┆ str  ┆ str           │
╞══════╪══════╪═══════════════╡
│ a    ┆ aa   ┆ 103:: ::a::aa │
│ b    ┆ bb   ┆ 103:: ::b::bb │
│ c    ┆ cc   ┆ 103:: ::c::cc │
│ d    ┆ dd   ┆ 103:: ::d::dd │
```

**💡 יתרונות:**
- תמיכה בביטויים מורכבים
- מפריד גמיש
- טיפול אוטומטי ב-NULL values

---

### 🔗 6.4 שרשור כל הערכים בעמודה

```python
# שרשור כל הערכים בעמודה לטקסט אחד
result = df.select(
    pl.all(),
    pl.col('colA').str.join(delimiter=', ').alias('concatenatedColA')
)
```

**פלט צפוי:**
```
shape: (4, 3)
│ colA ┆ colB ┆ concatenatedColA │
│ ---  ┆ ---  ┆ ---              │
│ str  ┆ str  ┆ str              │
╞══════╪══════╪══════════════════╡
│ a    ┆ aa   ┆ a, b, c, d       │
│ b    ┆ bb   ┆ a, b, c, d       │
│ c    ┆ cc   ┆ a, b, c, d       │
│ d    ┆ dd   ┆ a, b, c, d       │
```

**💡 שימוש נפוץ:**
- יצירת רשימות מופרדות בפסיקים
- סיכום נתונים
- דו"חות טקסטואליים

---

## 🎓 תרגילים מומלצים

### תרגיל 1: ניקוי וסינון נתונים
**משימה:** טען קובץ CSV עם ביקורות, נקה רווחים, המר לאותיות קטנות, וסנן רק ביקורות חיוביות.

```python
# פתרון מומלץ
result = (
    pl.read_csv('reviews.csv')
    .select(
        pl.col('content')
        .str.strip_chars()
        .str.to_lowercase()
        .alias('clean_content')
    )
    .filter(
        pl.col('clean_content')
        .str.contains_any(['excellent', 'great', 'amazing'])
    )
)
```

---

### תרגיל 2: חילוץ מידע מטקסט
**משימה:** חלץ תאריכים מטקסט חופשי.

```python
# חילוץ תאריכים בפורמט DD/MM/YYYY
result = df.select(
    'text',
    pl.col('text')
    .str.extract_all(r'(\d{2}/\d{2}/\d{4})')
    .alias('dates')
)
```

---

### תרגיל 3: עיבוד שמות
**משימה:** פצל שמות מלאים לשם פרטי ומשפחה.

```python
result = df.select(
    'full_name',
    pl.col('full_name')
    .str.split(by=' ')
    .list.get(0)
    .alias('first_name'),
    pl.col('full_name')
    .str.split(by=' ')
    .list.get(-1)
    .alias('last_name')
)
```

---

## 💡 טיפים וטריקים

### 1. ביצועים
✅ **עשה:** השתמש ב-`literal=True` כאשר לא צריך Regex
❌ **אל תעשה:** אל תשתמש ב-Regex מורכב ללא צורך

```python
# מהיר
df.filter(pl.col('text').str.contains('hello', literal=True))

# איטי יותר
df.filter(pl.col('text').str.contains('hello'))
```

---

### 2. טיפול ב-NULL
```python
# החלף NULL בערך ברירת מחדל
df.select(
    pl.col('text')
    .fill_null('')
    .str.to_uppercase()
)
```

---

### 3. שרשור ביטויים
```python
# ניקוי מקיף בשורה אחת
df.select(
    pl.col('text')
    .str.strip_chars()
    .str.to_lowercase()
    .str.replace_all(r'\s+', ' ')  # החלף רווחים מרובים ברווח אחד
)
```

---

### 4. דיבאג וולידציה
```python
# בדוק אורכי מחרוזות
df.select(
    pl.col('text').str.len_chars().describe()
)

# מצא מחרוזות חריגות
df.filter(
    (pl.col('text').str.len_chars() < 5) |
    (pl.col('text').str.len_chars() > 1000)
)
```

---

### 5. עבודה עם אימוג'ים ותווים מיוחדים
```python
# הסר אימוג'ים
df.select(
    pl.col('text')
    .str.replace_all(r'[^\x00-\x7F]+', '')
    .alias('clean_text')
)
```

---

## 📚 משאבים נוספים

### תיעוד רשמי
- [Polars String Documentation](https://pola-rs.github.io/polars/py-polars/html/reference/expressions/string.html)
- [Polars User Guide](https://pola-rs.github.io/polars-book/)

### למידה מתקדמת
- ביטויים רגולריים: [Regex101](https://regex101.com/)
- Polars Performance Guide
- String Processing Best Practices

### קהילה
- [Polars Discord](https://discord.com/invite/4UfP5cfBE7)
- [Stack Overflow - Polars Tag](https://stackoverflow.com/questions/tagged/python-polars)
- [Polars GitHub](https://github.com/pola-rs/polars)

---

## 📝 סיכום

בפרק זה למדנו:
- ✅ סינון מחרוזות עם תנאים מורכבים
- ✅ המרת מחרוזות לתאריכים ושעות
- ✅ חילוץ מידע עם Regex
- ✅ ניקוי וטיפול במחרוזות
- ✅ פיצול ושרשור מחרוזות
- ✅ טכניקות מתקדמות וטיפים

**המשך לתרגל ולהתנסות!** 🚀

---

**נוצר עבור:** לומדי Polars בעברית  
**גרסה:** 1.0  
**תאריך:** 2024
